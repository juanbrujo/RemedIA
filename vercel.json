# Vercel configuration for RemedIA (Nuxt 4)

{
  "version": 2,
  "builds": [
    { "src": "nuxt.config.ts", "use": "@nuxt/vercel-builder" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/" }
  ]
}
